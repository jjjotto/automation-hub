namespace AutomationHub.Watchers.Models;

/// <summary>
/// Legacy placeholder retained to avoid breaking binary compatibility with older builds.
/// Instrument-specific heuristics now live entirely in user-provided file filters.
/// </summary>
internal static class InstrumentMetadata
{
}
